<?php 
//phpinfo();
//$now = new DateTime();
//if (($now->format('H:i') >= "10:25") && ($now->format('H:i') < "10:27")) echo "true";
/*$disk_space = shell_exec("df -h | awk ' { print $1\"\t\"$5 } '");
$partition_use = preg_split( "/\n|\t/", $disk_space);
for ($i = 1; $i < (count($partition_use) / 2) - 1; $i++) {
   $partition_name = $partition_use[2 * $i];
   $used_space = (int) rtrim($partition_use[2 * $i + 1], "%\n");
   if ($used_space >= 70) {
     array_push($partition, $partition_name);
     array_push($space, $used_space);
   }
}*/
echo "Aissa";

?>
